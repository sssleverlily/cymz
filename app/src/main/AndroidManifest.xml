<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zia.gankcqupt_mvp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon300"
        android:label="@string/app_name"
        android:persistent="true"
        android:roundIcon="@mipmap/icon300"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.Activity.Page.StartActivity"
            android:theme="@style/SplashTheme"></activity>
        <activity android:name=".view.Activity.Page.MainActivity" />
        <activity
            android:name=".view.Activity.Page.DetailActivity"
            android:theme="@style/AppTheme.Slide" />
        <activity
            android:name=".view.Activity.Page.RecyclerActivity"
            android:theme="@style/AppTheme.Slide" />
        <activity android:name=".view.Activity.Page.LoginActivity" />
        <activity
            android:name=".view.Activity.Page.RegisterActivity"
            android:theme="@style/Translucent" />
        <activity
            android:name=".ui.start.StartActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity"></activity>
    </application>

</manifest>